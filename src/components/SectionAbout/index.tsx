import React from 'react'

import {
    HeroContainer
} from './SectionAboutElements'

const SectionAbout = () => {
    return (
        <HeroContainer id="about">
            About
        </HeroContainer>
    )
}

export default SectionAbout
